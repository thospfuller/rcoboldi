Initialize               Instantiates this package and must be called exactly once.
ReadCopyBookAsDataFrame  Converts the inFile into a data frame.
CobolToCSV               Converts the inFile into a CSV file and saves the result to the specified file.
About                    Prints some information about this plugin.
